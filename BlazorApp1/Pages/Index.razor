@page "/"
@inject NavigationManager Navigation
@code {
    @using Microsoft.AspNetCore.Components
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/Registration", forceLoad: false);
    }
}