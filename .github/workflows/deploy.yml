name: CI-CD Deploy

on:
  push:
    branches:
      - main

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Gateway API
        run: docker build -t gateway.api -f Gateway.API/Dockerfile .

      - name: Build Chess Game API
        run: docker build -t chessgame.api -f webAssemblyChessGame.Services.ChessGameAPI/Dockerfile .

      - name: Build Blazor Client
        run: docker build -t blazor.client -f BlazorServerSideClient/Dockerfile .

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PUBLIC_KEY }}
          script: |
            cd /root/ChessGameApplication
            git pull origin main
            docker compose down
            docker compose up --build -d